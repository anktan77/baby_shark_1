<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.baby_shark">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Baby_shark">
        <activity android:name=".RegisterOwnerStadium"></activity>
        <activity android:name=".LoginOwnerStadium" />
        <activity android:name=".Register" />
        <activity
            android:name=".OwnerStadiumActivity"
            android:label="@string/title_activity_owner_stadium"
            android:theme="@style/Theme.Baby_shark.NoActionBar" />
        <activity
            android:name=".BoookStadiumActivity"
            android:label="@string/title_activity_boook_stadium"
            android:theme="@style/Theme.Baby_shark.NoActionBar" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_api_key" />

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>